{
  "name": "spec-decode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provably exact speculative decoding — core algorithm in TypeScript.",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
